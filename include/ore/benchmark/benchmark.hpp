/**
 * @file benchmark.hpp
 * @brief Umbrella header for the `ore::benchmark` module.
 *
 * Pulls in the public API — cases, runner, report — so downstream
 * translation units can `#include <ore/benchmark/benchmark.hpp>`
 * and be done with it.
 */
#pragma once

#include <ore/benchmark/benchmark_case.hpp>
#include <ore/benchmark/benchmark_runner.hpp>
