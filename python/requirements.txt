# Milestone 2 dependencies.
#
# Pinned to floors that support the APIs the downloaders use. Bump
# deliberately (never blindly).
yfinance>=0.2.40,<0.3
pandas>=2.0,<3.0

# Research-phase analysis scripts (calibration_research.py, iv_rv_study.py,
# iv_smile_forecast_study.py): read the generated study CSVs and run the
# regressions, statistics, and figures.
numpy>=1.24
matplotlib>=3.7
scipy>=1.10

# Milestone-5 Greek-information study (greek_information_study.py) uses
# LASSO/ElasticNet/RandomForest + permutation importance.
scikit-learn>=1.3
